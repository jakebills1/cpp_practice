#include <iostream> //preprocessor directive to include i/o utilities
// main function is where every c++ program starts
int main()
{
    std::cout << "Welcome to C++\n";
    // cout belongs to the namespace std, whhich was brought into the program by iostream
    // \n is the escape character for newline, which prints a newline to stdout
    return 0;
    // indicates a successful end of program
}
